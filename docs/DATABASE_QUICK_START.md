# Быстрый старт - База данных устройств

Этот документ поможет быстро понять и начать использовать концепцию базы данных устройств.

## 📚 Документация

Для изучения концепции рекомендуется следующий порядок:

1. **Этот документ** (DATABASE_QUICK_START.md) - краткий обзор
2. **DATABASE_ER_DIAGRAM.txt** - визуальная схема БД
3. **DATABASE_CONCEPT.md** - полная концепция (основной документ)
4. **DATABASE_IMPLEMENTATION_PLAN.md** - план реализации
5. **device_database_schema.sql** - SQL схема с комментариями
6. **init_device_db_sample_data.sql** - примеры данных

## 🎯 Основная идея

База данных построена по иерархическому принципу:

```
Категории → Типы устройств → Устройства → Параметры
```

**Пример:**
```
Электроаппараты → Автоматические выключатели → ABB S203 C16 → [Ток: 16А, Характеристика: C, ...]
```

## 🔑 Ключевые таблицы

### 1. Categories (Категории)
```sql
Электроаппараты, Кабели, ОПС, Автоматика, ...
```

### 2. DeviceTypes (Типы устройств)
```sql
Автоматы, УЗО, Контакторы, Кабели силовые, Кабели контрольные, ...
```

### 3. Devices (Устройства)
```sql
ABB S203 C16, Schneider iC60N C25, ВВГнг 3х2.5, ...
```

### 4. ParameterDefinitions (Определения параметров)
```sql
Для автоматов: ток_срабатывания, характеристика, отключающая_способность, ...
Для кабелей: количество_жил, сечение, ДДТ, тип_изоляции, ...
```

### 5. DeviceParameters (Значения параметров)
```sql
ABB S203 C16:
  - ток_срабатывания = 16 А
  - характеристика = "C"
  - отключающая_способность = 6 кА
```

## 💡 Примеры использования

### Пример 1: Найти все автоматы на 16А

```sql
SELECT manufacturer, model, part_number, price
FROM v_DeviceFullInfo
WHERE device_type_code = 'BREAKER'
  AND rated_current = 16
ORDER BY price;
```

**Результат:**
```
IEK    | ВА47-29    | MVA20-3-016-C | 185.00
ABB    | S203       | S203-C16      | 850.00
Schneider | iC60N   | A9F74316      | 780.00
```

### Пример 2: Получить все параметры устройства

```sql
SELECT parameter_name, value_display, unit
FROM v_DeviceParametersComplete
WHERE device_id = 1
ORDER BY sort_order;
```

**Результат:**
```
Ток срабатывания         | 16  | А
Характеристика           | C   |
Отключающая способность  | 6   | кА
Количество полюсов       | 3   | шт
```

### Пример 3: Поиск по производителю и модели

```sql
SELECT * FROM Devices
WHERE manufacturer LIKE '%ABB%'
  AND model LIKE '%S203%';
```

### Пример 4: Найти кабель под автомат 16А

```sql
-- Найти совместимые кабели
SELECT d.manufacturer, d.model, dp.value_numeric as ddt
FROM DeviceCompatibility dc
JOIN Devices d ON dc.device_id = d.id
JOIN DeviceParameters dp ON d.id = dp.device_id
JOIN ParameterDefinitions pd ON dp.parameter_def_id = pd.id
WHERE dc.compatible_device_id = 1  -- ABB S203 C16
  AND pd.code = 'continuous_current'
  AND dc.compatibility_type = 'cable-breaker';
```

## 🚀 Как добавить новое устройство

### Шаг 1: Убедиться, что категория и тип существуют

```sql
-- Проверить категории
SELECT * FROM Categories;

-- Проверить типы
SELECT * FROM DeviceTypes WHERE category_id = 1;
```

### Шаг 2: Добавить устройство

```sql
INSERT INTO Devices (
    device_type_id,
    manufacturer,
    model,
    part_number,
    name_short,
    name_full,
    rated_voltage,
    rated_current,
    price
) VALUES (
    1,                              -- ID типа "Автоматические выключатели"
    'Legrand',
    'TX3',
    'TX3-C16',
    'Legrand TX3 C16',
    'Автоматический выключатель Legrand TX3, C, 16А, 3P',
    400,
    16,
    720.00
);
```

### Шаг 3: Добавить параметры

```sql
-- Узнать ID только что добавленного устройства
SELECT last_insert_rowid();  -- Допустим, получили id = 10

-- Добавить параметры
INSERT INTO DeviceParameters (device_id, parameter_def_id, value_numeric, value_text)
VALUES
    (10, 1, 16, NULL),       -- Ток срабатывания
    (10, 2, NULL, 'C'),      -- Характеристика
    (10, 3, 6, NULL),        -- Отключающая способность
    (10, 4, 3, NULL);        -- Количество полюсов
```

Готово! Устройство добавлено в базу данных.

## 🔧 Как добавить новый тип устройства

### Пример: Добавим "Пускатели"

```sql
-- Шаг 1: Добавить тип устройства
INSERT INTO DeviceTypes (
    category_id,
    name,
    code,
    has_rated_current,
    has_voltage
) VALUES (
    1,                  -- Категория "Электроаппараты"
    'Пускатели',
    'STARTER',
    1,                  -- Имеет номинальный ток
    1                   -- Имеет напряжение
);

-- Узнаем ID нового типа
SELECT last_insert_rowid();  -- Допустим, получили id = 11

-- Шаг 2: Определить специфичные параметры для пускателей
INSERT INTO ParameterDefinitions (device_type_id, name, code, data_type, unit, is_required, sort_order)
VALUES
    (11, 'Категория применения', 'application_category', 'TEXT', '', 1, 1),
    (11, 'Номинальный ток контактора', 'contactor_current', 'REAL', 'А', 1, 2),
    (11, 'Номинальный ток теплового реле', 'thermal_relay_current', 'REAL', 'А', 1, 3),
    (11, 'Мощность при 400В', 'power_400v', 'REAL', 'кВт', 0, 4);

-- Шаг 3: Добавить конкретные пускатели
INSERT INTO Devices (
    device_type_id,
    manufacturer,
    model,
    name_short,
    rated_voltage,
    rated_current,
    price
) VALUES (
    11,
    'Schneider Electric',
    'LE1M35N712',
    'Пускатель LE1M35',
    400,
    12,
    3500.00
);
```

## 📊 Полезные запросы

### Получить иерархию всех устройств

```sql
SELECT * FROM v_CategoryHierarchy;
```

### Посчитать устройства по производителям

```sql
SELECT manufacturer, COUNT(*) as count, AVG(price) as avg_price
FROM Devices
WHERE is_active = 1
GROUP BY manufacturer
ORDER BY count DESC;
```

### Найти самые дешевые устройства каждого типа

```sql
SELECT dt.name as device_type,
       d.manufacturer,
       d.model,
       d.price
FROM Devices d
JOIN DeviceTypes dt ON d.device_type_id = dt.id
WHERE d.price = (
    SELECT MIN(price)
    FROM Devices d2
    WHERE d2.device_type_id = d.device_type_id
      AND d2.is_active = 1
)
ORDER BY dt.name;
```

### Полнотекстовый поиск устройств

```sql
-- Искать "ABB 16 автомат"
SELECT d.manufacturer, d.model, d.name_full
FROM DeviceSearch ds
JOIN Devices d ON ds.device_id = d.id
WHERE DeviceSearch MATCH 'ABB AND 16 AND автомат*';
```

## ⚡ Оптимизация производительности

### Использование индексов

База данных уже содержит индексы для:
- Поиска по производителю и модели
- Фильтрации по току и напряжению
- Связей между таблицами
- Полнотекстового поиска

### Кэширование часто используемых данных

При интеграции с Pascal кодом рекомендуется кэшировать:
- Список категорий и типов (загружается один раз)
- Определения параметров для типов
- Часто используемые устройства

### Пагинация результатов

Для больших списков используйте LIMIT и OFFSET:

```sql
-- Получить 50 устройств, начиная с 100-го
SELECT * FROM v_DeviceFullInfo
ORDER BY manufacturer, model
LIMIT 50 OFFSET 100;
```

## 🎓 Расширенные возможности

### Варианты устройств

Один базовый автомат, но разные токи:

```sql
-- Базовое устройство: ABB S203
-- Варианты: S203-C10, S203-C16, S203-C25, S203-C32

INSERT INTO DeviceVariants (parent_device_id, variant_name, part_number, price_modifier)
VALUES
    (1, '10А', 'S203-C10', -50),
    (1, '25А', 'S203-C25', 50),
    (1, '32А', 'S203-C32', 100);
```

### Совместимость устройств

Указать, какие кабели подходят к автоматам:

```sql
INSERT INTO DeviceCompatibility (device_id, compatible_device_id, compatibility_type, compatibility_notes)
VALUES
    (7, 1, 'cable-breaker', 'ВВГнг 3х2.5 (25А) подходит для автомата 16А'),
    (7, 2, 'cable-breaker', 'ВВГнг 3х2.5 (25А) подходит для автомата 25А');
```

## 🔍 Отладка и проверка

### Проверить целостность данных

```sql
-- Устройства без параметров
SELECT d.id, d.manufacturer, d.model
FROM Devices d
LEFT JOIN DeviceParameters dp ON d.id = dp.device_id
WHERE dp.id IS NULL;

-- Параметры без определений
SELECT dp.id, dp.device_id
FROM DeviceParameters dp
LEFT JOIN ParameterDefinitions pd ON dp.parameter_def_id = pd.id
WHERE pd.id IS NULL;

-- Устройства без типа
SELECT d.id, d.manufacturer, d.model
FROM Devices d
LEFT JOIN DeviceTypes dt ON d.device_type_id = dt.id
WHERE dt.id IS NULL;
```

### Статистика базы данных

```sql
SELECT 'Категорий' as info, COUNT(*) as count FROM Categories
UNION ALL SELECT 'Типов устройств', COUNT(*) FROM DeviceTypes
UNION ALL SELECT 'Устройств', COUNT(*) FROM Devices
UNION ALL SELECT 'Определений параметров', COUNT(*) FROM ParameterDefinitions
UNION ALL SELECT 'Значений параметров', COUNT(*) FROM DeviceParameters
UNION ALL SELECT 'Связей совместимости', COUNT(*) FROM DeviceCompatibility
UNION ALL SELECT 'Вариантов', COUNT(*) FROM DeviceVariants;
```

## 📖 Дальнейшее чтение

- **DATABASE_CONCEPT.md** - полная концепция с подробными объяснениями
- **DATABASE_IMPLEMENTATION_PLAN.md** - как реализовать интеграцию с программой
- **device_database_schema.sql** - полная SQL схема с комментариями
- **DATABASE_ER_DIAGRAM.txt** - визуальная схема связей

## ❓ Частые вопросы

### Как добавить параметр, которого нет в ParameterDefinitions?

1. Добавить определение параметра:
```sql
INSERT INTO ParameterDefinitions (device_type_id, name, code, data_type, unit)
VALUES (1, 'Новый параметр', 'new_param', 'TEXT', 'ед');
```

2. Добавить значение для устройства:
```sql
INSERT INTO DeviceParameters (device_id, parameter_def_id, value_text)
VALUES (1, last_insert_rowid(), 'Значение');
```

### Как сделать параметр общим для всех типов?

Установить `device_type_id = NULL` в ParameterDefinitions:

```sql
INSERT INTO ParameterDefinitions (device_type_id, name, code, data_type)
VALUES (NULL, 'Вес', 'weight', 'REAL');
```

### Как найти устройства в определенном ценовом диапазоне?

```sql
SELECT * FROM v_DeviceFullInfo
WHERE price BETWEEN 500 AND 1000
ORDER BY price;
```

### Как изменить цену устройства?

```sql
UPDATE Devices
SET price = 950.00
WHERE id = 1;
```

### Как архивировать устройство (не удалять)?

```sql
UPDATE Devices
SET is_active = 0
WHERE id = 1;
```

Устройство останется в БД, но не будет показываться в выборках с фильтром `is_active = 1`.

## 🎉 Заключение

Эта база данных позволяет:

✅ Хранить любые типы устройств
✅ Добавлять произвольные параметры
✅ Быстро искать и фильтровать
✅ Легко расширять новыми типами
✅ Поддерживать совместимость устройств
✅ Работать с вариантами исполнения

Приятной работы! 🚀
