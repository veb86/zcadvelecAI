# План реализации базы данных устройств

## Обзор

Этот документ описывает пошаговый план внедрения базы данных устройств в проект zcadvelecAI.

## Фаза 1: Подготовка (1-2 дня)

### 1.1 Создание SQL схемы
- [ ] Создать файл `device_database_schema.sql` с полной схемой БД
- [ ] Добавить комментарии к таблицам и полям на русском языке
- [ ] Создать скрипт инициализации `init_device_db.sql`

### 1.2 Тестовая БД
- [ ] Создать тестовую БД SQLite с примерами данных
- [ ] Добавить 2-3 категории
- [ ] Добавить 5-7 типов устройств
- [ ] Добавить 10-15 примеров устройств с параметрами

### 1.3 Документация
- [x] Создать концепцию БД (DATABASE_CONCEPT.md)
- [x] Создать план реализации (этот документ)
- [ ] Создать ER-диаграмму базы данных

## Фаза 2: Интеграция с кодом (3-5 дней)

### 2.1 Модуль работы с БД
Создать Pascal модуль `uzcdevicedb.pas` со следующим функционалом:

```pascal
unit uzcdevicedb;

interface
uses
  Classes, SysUtils, SQLDB, SQLite3Conn;

type
  // Класс для работы с БД устройств
  TDeviceDatabase = class
  private
    FConnection: TSQLite3Connection;
    FTransaction: TSQLTransaction;
    FDatabasePath: string;
  public
    constructor Create(const ADatabasePath: string);
    destructor Destroy; override;

    // Управление соединением
    procedure Connect;
    procedure Disconnect;

    // Категории
    function GetCategories: TStringList;
    function AddCategory(const AName, ACode: string): Integer;

    // Типы устройств
    function GetDeviceTypes(ACategoryID: Integer): TStringList;
    function AddDeviceType(const ACategoryID: Integer; const AName, ACode: string): Integer;

    // Устройства
    function SearchDevices(const ASearchText: string): TDataSet;
    function GetDevice(ADeviceID: Integer): TDataSet;
    function AddDevice(/* параметры */): Integer;
    function UpdateDevice(ADeviceID: Integer /* параметры */): Boolean;
    function DeleteDevice(ADeviceID: Integer): Boolean;

    // Параметры
    function GetDeviceParameters(ADeviceID: Integer): TDataSet;
    function SetDeviceParameter(ADeviceID, AParameterDefID: Integer;
                                const AValue: Variant): Boolean;

    // Вспомогательные методы
    function ExecuteQuery(const ASQL: string): TSQLQuery;
    procedure ExecuteNonQuery(const ASQL: string);
  end;

implementation
// ... реализация
end.
```

### 2.2 Интеграция с существующей системой
- [ ] Расширить `DeviceDbBaseObject` для работы с новой БД
- [ ] Добавить методы загрузки/сохранения в БД
- [ ] Реализовать кэширование часто используемых данных

### 2.3 Адаптер для обратной совместимости
- [ ] Создать адаптер для загрузки из старых .pas/.csv файлов
- [ ] Создать миграционный скрипт для переноса данных

## Фаза 3: Пользовательский интерфейс (5-7 дней)

### 3.1 Форма управления БД устройств
Создать форму `TfrmDeviceDatabase` с вкладками:

**Вкладка "Категории и типы"**:
- TreeView с иерархией Категория → Тип устройства
- Кнопки добавления/редактирования/удаления
- Drag & drop для изменения порядка

**Вкладка "Устройства"**:
- Grid с списком устройств (с фильтрацией и сортировкой)
- Панель быстрого поиска
- Кнопки CRUD операций

**Вкладка "Параметры"**:
- Список параметров выбранного устройства
- Редактор значений с проверкой типов
- Добавление пользовательских параметров

### 3.2 Диалог добавления/редактирования устройства
```
┌─────────────────────────────────────────────────────┐
│ Добавить устройство                            [X]  │
├─────────────────────────────────────────────────────┤
│ Основная информация                                 │
│ Категория:    [▼ Электроаппараты        ]          │
│ Тип:          [▼ Автоматические выключатели]        │
│ Производитель:[ABB                      ]          │
│ Модель:       [S203                     ]          │
│ Артикул:      [S203-C16                 ]          │
│                                                     │
│ Электрические параметры                             │
│ Ном. напряжение: [400     ] В                      │
│ Ном. ток:        [16      ] А                      │
│                                                     │
│ Специфичные параметры                               │
│ ┌───────────────────────────────────────┐          │
│ │ Характеристика:        [C    ]        │          │
│ │ Отключающая способность:[6    ] кА    │          │
│ │ Количество полюсов:    [3    ]        │          │
│ └───────────────────────────────────────┘          │
│                                                     │
│              [Сохранить] [Отмена]                  │
└─────────────────────────────────────────────────────┘
```

### 3.3 Интеграция в главное меню
- [ ] Добавить пункт меню "База данных → Устройства"
- [ ] Добавить кнопку на панель инструментов
- [ ] Добавить контекстное меню для быстрого доступа

## Фаза 4: Импорт/Экспорт (2-3 дня)

### 4.1 Импорт данных
- [ ] Импорт из Excel (.xlsx)
  - Шаблон для массового импорта
  - Валидация данных при импорте
  - Отчет об ошибках
- [ ] Импорт из CSV
- [ ] Импорт из старого формата (.pas файлов)
- [ ] Импорт из каталогов производителей

### 4.2 Экспорт данных
- [ ] Экспорт в Excel (с группировкой по категориям)
- [ ] Экспорт в CSV
- [ ] Экспорт в PDF (каталог устройств)
- [ ] Экспорт подмножества (по фильтру)

### 4.3 Шаблоны
```excel
Шаблон Excel для импорта:
| Категория | Тип | Производитель | Модель | Артикул | Напряжение | Ток | Параметр1 | Параметр2 |...
```

## Фаза 5: Расширенные функции (5-7 дней)

### 5.1 Автоматический подбор устройств
```pascal
// Подбор автомата по параметрам кабеля
function SelectBreaker(ACableCurrentCapacity: Double;
                      AVoltage: Double): TDeviceList;

// Подбор кабеля по нагрузке
function SelectCable(ALoadCurrent: Double;
                    ALength: Double;
                    AInstallMethod: string): TDeviceList;
```

### 5.2 Проверка совместимости
- [ ] Таблица совместимости устройств
- [ ] Автоматическая проверка при добавлении в схему
- [ ] Предупреждения о несовместимости

### 5.3 Расчеты
- [ ] Расчет падения напряжения на кабеле
- [ ] Расчет селективности защиты
- [ ] Расчет токов КЗ
- [ ] Проверка соответствия ПУЭ

### 5.4 Отчеты и спецификации
- [ ] Автоматическое формирование спецификации из чертежа
- [ ] Подсчет стоимости проекта
- [ ] Экспорт в форматы ГОСТ (сметы, спецификации)

## Фаза 6: Оптимизация и тестирование (3-5 дней)

### 6.1 Производительность
- [ ] Профилирование запросов к БД
- [ ] Оптимизация индексов
- [ ] Кэширование часто используемых данных
- [ ] Тестирование на большом объеме данных (10000+ устройств)

### 6.2 Надежность
- [ ] Unit-тесты для модуля БД
- [ ] Тесты целостности данных
- [ ] Обработка ошибок и исключений
- [ ] Автоматическое резервное копирование

### 6.3 Пользовательское тестирование
- [ ] Тестирование UI на удобство
- [ ] Проверка импорта реальных данных
- [ ] Тестирование производительности поиска
- [ ] Сбор обратной связи

## Фаза 7: Документация и обучение (2-3 дня)

### 7.1 Техническая документация
- [ ] API документация модуля БД
- [ ] Описание структуры БД
- [ ] Примеры SQL запросов
- [ ] Руководство по расширению

### 7.2 Пользовательская документация
- [ ] Руководство пользователя
- [ ] Примеры использования
- [ ] Видеоуроки (опционально)
- [ ] FAQ

### 7.3 Справочные данные
- [ ] Готовая БД с типовыми устройствами
  - 100+ автоматических выключателей
  - 50+ типов кабелей
  - Приборы ОПС
  - Кабеленесущие системы

## Фаза 8: Развертывание (1-2 дня)

### 8.1 Подготовка к релизу
- [ ] Финальное тестирование
- [ ] Создание инсталлятора с БД
- [ ] Подготовка Release Notes
- [ ] Обновление CHANGELOG

### 8.2 Миграция
- [ ] Скрипт миграции для существующих пользователей
- [ ] Инструкция по миграции данных
- [ ] Резервное копирование перед миграцией

### 8.3 Поддержка
- [ ] Мониторинг ошибок
- [ ] Сбор обратной связи
- [ ] Быстрые исправления (hotfixes)

## Технические детали

### Зависимости
```pascal
// Требуемые пакеты Free Pascal/Lazarus
uses
  SQLDB,           // Универсальный доступ к БД
  SQLite3Conn,     // SQLite коннектор
  sqlite3dyn,      // Динамическая загрузка SQLite
  DB,              // Базовые классы для работы с данными
  DBGrids;         // Компоненты для отображения данных
```

### Файловая структура
```
cad_source/zcad/
├── devicebase/
│   ├── uzcdevicebase.pas           (существует)
│   ├── uzcdevicebaseabstract.pas   (существует)
│   ├── uzcdevicedb.pas             (новый - модуль работы с БД)
│   └── uzcdevicedbgui.pas          (новый - UI компоненты)
├── database/
│   ├── device_database_schema.sql  (новый - схема БД)
│   ├── init_device_db.sql          (новый - инициализация)
│   └── migration_scripts/          (новый - миграции)
└── forms/
    ├── uzcfdevicedb.pas/.lfm       (новый - главная форма БД)
    └── uzcfdeviceedit.pas/.lfm     (новый - редактор устройства)

/devicelib/                          (директория с БД)
├── devices.db                       (основная БД)
├── devices.db.backup                (резервная копия)
└── templates/                       (шаблоны импорта)
    ├── import_template.xlsx
    └── import_template.csv
```

### Конфигурация
```pascal
// В uzcsysvars.pas добавить:
type
  TDatabaseSettings = record
    DatabasePath: String;
    AutoBackup: Boolean;
    BackupInterval: Integer; // в днях
    CacheSize: Integer;
    EnableFullTextSearch: Boolean;
  end;

var
  DatabaseSettings: TDatabaseSettings;
```

## Метрики успеха

### Производительность
- ⚡ Поиск устройства < 100 мс
- ⚡ Загрузка списка 1000 устройств < 500 мс
- ⚡ Добавление устройства < 50 мс

### Юзабилити
- 👤 Добавление нового устройства за < 2 минуты
- 👤 Поиск нужного устройства за < 10 секунд
- 👤 Интуитивно понятный интерфейс

### Надежность
- 🔒 Нет потери данных
- 🔒 Автоматическое резервное копирование
- 🔒 Откат изменений при ошибке

## Риски и их минимизация

### Риск 1: Производительность при большом объеме данных
**Минимизация**:
- Использовать индексы
- Кэширование
- Пагинация результатов

### Риск 2: Сложность миграции данных
**Минимизация**:
- Подробная документация миграции
- Автоматические скрипты
- Резервное копирование

### Риск 3: Кривая обучения для пользователей
**Минимизация**:
- Интуитивный интерфейс
- Подробная документация
- Видеоуроки
- Примеры данных

### Риск 4: Конфликты версий БД
**Минимизация**:
- Версионирование схемы БД
- Автоматические миграции
- Проверка совместимости

## Временная оценка

| Фаза | Оценка времени | Приоритет |
|------|----------------|-----------|
| Фаза 1: Подготовка | 1-2 дня | Высокий |
| Фаза 2: Интеграция | 3-5 дней | Высокий |
| Фаза 3: UI | 5-7 дней | Высокий |
| Фаза 4: Импорт/Экспорт | 2-3 дня | Средний |
| Фаза 5: Расширенные функции | 5-7 дней | Средний |
| Фаза 6: Тестирование | 3-5 дней | Высокий |
| Фаза 7: Документация | 2-3 дня | Средний |
| Фаза 8: Развертывание | 1-2 дня | Высокий |
| **ИТОГО** | **22-34 дня** | |

## Следующие шаги

1. **Обсуждение концепции** - получить обратную связь от команды
2. **Согласование приоритетов** - определить must-have и nice-to-have функции
3. **Выделение ресурсов** - определить исполнителей для каждой фазы
4. **Создание прототипа** - реализовать минимальный работающий прототип
5. **Итеративная разработка** - постепенное добавление функционала

## Контакты и обратная связь

Для вопросов и предложений по реализации базы данных устройств используйте:
- GitHub Issues: https://github.com/veb86/zcadvelecAI/issues/220
- Обсуждения в Pull Request
